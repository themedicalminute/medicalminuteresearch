# The Medical Minute — Research Based Essays

Static site (GitHub Pages). Entry point: `index.html`.

Pages: index (welcome + essay catalog), officers (Who We Are + About Us), join, socials.

Local preview: `node server.js` then open http://localhost:5560
